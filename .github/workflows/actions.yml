name: CI for GitHub Actions
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint     
      
  test:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - name: Testing the code
        run: |
          cd /home/runner/work/first
          npm run test

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Building the project
        run: vite build